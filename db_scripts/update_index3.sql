create table SIG_US (
       day date NOT NULL,
       high decimal(7,2) NOT NULL,
       low decimal(7,2) NOT NULL,
       closing decimal(7,2) NOT NULL,
       volume int(11) DEFAULT NULL,
       PRIMARY KEY (day)
);
create table PYPL_US (
       day date NOT NULL,
       high decimal(7,2) NOT NULL,
       low decimal(7,2) NOT NULL,
       closing decimal(7,2) NOT NULL,
       volume int(11) DEFAULT NULL,
       PRIMARY KEY (day)
);
create table AAP_US (
       day date NOT NULL,
       high decimal(7,2) NOT NULL,
       low decimal(7,2) NOT NULL,
       closing decimal(7,2) NOT NULL,
       volume int(11) DEFAULT NULL,
       PRIMARY KEY (day)
);
create table CPGX_US (
       day date NOT NULL,
       high decimal(7,2) NOT NULL,
       low decimal(7,2) NOT NULL,
       closing decimal(7,2) NOT NULL,
       volume int(11) DEFAULT NULL,
       PRIMARY KEY (day)
);
create table JBHT_US (
       day date NOT NULL,
       high decimal(7,2) NOT NULL,
       low decimal(7,2) NOT NULL,
       closing decimal(7,2) NOT NULL,
       volume int(11) DEFAULT NULL,
       PRIMARY KEY (day)
);
create table BXLT_US (
       day date NOT NULL,
       high decimal(7,2) NOT NULL,
       low decimal(7,2) NOT NULL,
       closing decimal(7,2) NOT NULL,
       volume int(11) DEFAULT NULL,
       PRIMARY KEY (day)
);
create table QRVO_US (
       day date NOT NULL,
       high decimal(7,2) NOT NULL,
       low decimal(7,2) NOT NULL,
       closing decimal(7,2) NOT NULL,
       volume int(11) DEFAULT NULL,
       PRIMARY KEY (day)
);
create table O_US (
       day date NOT NULL,
       high decimal(7,2) NOT NULL,
       low decimal(7,2) NOT NULL,
       closing decimal(7,2) NOT NULL,
       volume int(11) DEFAULT NULL,
       PRIMARY KEY (day)
);
create table AAL_US (
	  day date NOT NULL,
	  high decimal(7,2) NOT NULL,
	  low decimal(7,2) NOT NULL,
	  closing decimal(7,2) NOT NULL,
	  volume int(11) DEFAULT NULL,
          PRIMARY KEY (day)
);
create table EQIX_US (
       day date NOT NULL,
       high decimal(7,2) NOT NULL,
       low decimal(7,2) NOT NULL,
       closing decimal(7,2) NOT NULL,
       volume int(11) DEFAULT NULL,
       PRIMARY KEY (day)
);
create table SLG_US (
       day date NOT NULL,
       high decimal(7,2) NOT NULL,
       low decimal(7,2) NOT NULL,
       closing decimal(7,2) NOT NULL,
       volume int(11) DEFAULT NULL,
       PRIMARY KEY (day)
);
create table HBI_US (
       day date NOT NULL,
       high decimal(7,2) NOT NULL,
       low decimal(7,2) NOT NULL,
       closing decimal(7,2) NOT NULL,
       volume int(11) DEFAULT NULL,
       PRIMARY KEY (day)
);
create table HSIC_US (
       day date NOT NULL,
       high decimal(7,2) NOT NULL,
       low decimal(7,2) NOT NULL,
       closing decimal(7,2) NOT NULL,
       volume int(11) DEFAULT NULL,
       PRIMARY KEY (day)
);
create table SWKS_US (
       day date NOT NULL,
       high decimal(7,2) NOT NULL,
       low decimal(7,2) NOT NULL,
       closing decimal(7,2) NOT NULL,
       volume int(11) DEFAULT NULL,
       PRIMARY KEY (day)
);
create table HCA_US (
       day date NOT NULL,
       high decimal(7,2) NOT NULL,
       low decimal(7,2) NOT NULL,
       closing decimal(7,2) NOT NULL,
       volume int(11) DEFAULT NULL,
       PRIMARY KEY (day)
);
create table ENDP_US (
       day date NOT NULL,
       high decimal(7,2) NOT NULL,
       low decimal(7,2) NOT NULL,
       closing decimal(7,2) NOT NULL,
       volume int(11) DEFAULT NULL,
       PRIMARY KEY (day)
);
create table RCL_US (
       day date NOT NULL,
       high decimal(7,2) NOT NULL,
       low decimal(7,2) NOT NULL,
       closing decimal(7,2) NOT NULL,
       volume int(11) DEFAULT NULL,
       PRIMARY KEY (day)
);

